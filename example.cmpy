def fib(n: int) -> int:
	if n == 1 or n == 0:
		return 1
	else:
		return fib(n - 1) + fib(n - 2)

def main() -> int:
	c: tuple<bool, int, float, string> = (True,2,3.0, "hello world")
	b: int = 0
	d: bool = False
	e: bool = False

	x: tuple<int, float>
	x = (4, 4.0)

	if True:
		a: int = 2
		if d:
			z: int = 4
			return a + z
		elif e:
			return b
		else:
			return b

	else:
		return b
