def fib(n: int) -> int:
	if n == 1 or n == 0:
		return 1
	else:
		return fib(n - 1) + fib(n - 2)

def foo() -> None:
	print("returning none")

def main() -> int:
	c: tuple<bool, int, float> = (True,2,3.0)
	b: int = 0

	if True:
		a: int = 2
		return a
	else:
		return b
